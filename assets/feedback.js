const key="news-assistant-events";const read=()=>JSON.parse(localStorage.getItem(key)||"{}");const write=x=>localStorage.setItem(key,JSON.stringify(x));document.querySelectorAll("article").forEach(card=>{const started=Date.now();card.querySelectorAll("[data-feedback]").forEach(button=>button.addEventListener("click",()=>{const data=read();const id=card.dataset.article;data[id]={topic:card.dataset.topic,source:card.dataset.source,feedback:button.dataset.feedback,dwellBucket:Math.floor((Date.now()-started)/1000)<10?"skim":Math.floor((Date.now()-started)/1000)<60?"read":"deep"};write(data)}))});document.querySelector("#export")?.addEventListener("click",()=>{const events=Object.values(read());const aggregate={topics:{},sources:{},feedback:{positive:0,negative:0}};events.forEach(e=>{aggregate.topics[e.topic]=(aggregate.topics[e.topic]||0)+1;aggregate.sources[e.source]=(aggregate.sources[e.source]||0)+1;if(e.feedback)aggregate.feedback[e.feedback]++});const blob=new Blob([JSON.stringify(aggregate,null,2)],{type:"application/json"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download="news-preferences.json";a.click();URL.revokeObjectURL(a.href)});
